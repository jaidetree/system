# Configure git to use personal SSH key (not Snowflake's sf __ssh)
# This automatically applies when entering the system repo directory
export GIT_SSH_COMMAND="/usr/bin/ssh -i $HOME/.ssh/id_ed_personal"
